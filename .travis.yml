language: php

sudo: false

php:
  - 5.5

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - mkdir -p build/logs
  - rm -rf app/cache/*/*

script: 
  - composer phpunit